<div class="@_class" @attributes="@AdditionalAttributes">
	@if (!string.IsNullOrEmpty(Title))
	{
        <GdsHeading Level="1" BaseCssClass="govuk-panel__title">
            @Title
		</GdsHeading>
	}

	@if (ChildContent is not null)
	{
		<div class="govuk-panel__body">
			@ChildContent
		</div>
	}

	@if (Actions is not null)
	{
		<div class="govuk-panel__actions">
			@Actions
		</div>
	}
</div>

@code {
	[Parameter, EditorRequired]
	public string? Title { get; set; }

	[Parameter]
	public RenderFragment? ChildContent { get; set; }

	[Parameter]
	public RenderFragment? Actions { get; set; }
	
	[Parameter]
	public string? AdditionalCssClasses { get; set; }

	[Parameter]
	public bool Interruption { get; set; }

	[Parameter(CaptureUnmatchedValues = true)]
	public IReadOnlyDictionary<string, object>? AdditionalAttributes { get; set; }

    private string? _class;

	protected override void OnParametersSet()
	{
		_class = new CssClassBuilder("govuk-panel")
			.AddIf(!Interruption, "govuk-panel--confirmation")
			.AddIf(Interruption, "govuk-panel--interruption")
			.Add(AdditionalCssClasses)
			.Build();
	}
}