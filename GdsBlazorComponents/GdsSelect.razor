@typeparam T
@inherits InputSelect<T>

<select class="@ComponentClass" @attributes="AdditionalAttributes" @bind="CurrentValue" @bind:event="onchange">
    @ChildContent
</select>

@code {
    private string ComponentClass => $"govuk-select {CssClass}".Trim();
}